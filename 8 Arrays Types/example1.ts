// [] Notation
const nums: number[] = [1, 2, 3, 4];
const str: string[] = ["one", "two", "three"];

const items: string[] = []
items.push("Keyboard")

const numList: number[] = []
numList.push(20);

console.log(numList);

// Array<type> Notation
